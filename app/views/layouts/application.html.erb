<!DOCTYPE html>
<html>
  <head>
    <title>CrewCoord</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- page specific css -->
    <%= yield :moreCSS %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- page specific JS -->
    <%= yield :moreJS %>
  </head>

  <body>
    <%= render 'layouts/top_nav_bar' %>
    <%= yield %>
  </body>
</html>
